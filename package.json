{
  "name": "ultra-agent-os",
  "version": "1.0.0",
  "description": "Ultra Agent OS - Railway Multi-Service Deployment",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "build": "echo "No build required"",
    "start": "concurrently -n api,worker,ui -c blue,green,yellow \"npm run start:api\" \"npm run start:worker\" \"npm run start:ui\"",
    "start:api": "cd apps/api && npm start",
    "start:worker": "cd apps/worker && npm start", 
    "start:ui": "cd apps/ui && npm start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
